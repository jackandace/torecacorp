# SP geometry, direct draw buttons and fixed detail controls

User screenshot feedback addressed:
- Equal mobile gutters; catalog and carousel use bounded percentage widths rather than viewport-based overflow. Category and sort have separate rows on mobile. Horizontal scrolling remains inside the category/banner rails; scrollbars are hidden.
- Stronger orange, pink and violet action colors; saturated campaign/rank accents over legible neutral surfaces.
- 1/10/100 buttons are directly visible in catalog and detail. Each opens the corresponding count preselected in the existing coin-consumption confirmation. No payment step added.
- Detail controls fixed at viewport bottom on PC/SP; padded document footer leaves final content reachable. Thumbnail is immediately followed by actual known daily-limit/shipping-condition text, then all rank groups and final cautions.
- Replaced every app reference to the M coin bitmap with an original code-native functional P coin SVG. No external brand used.

Verification: mobile CSS width 390, body/scroll width 390, catalog gutters 12px each. Fixed control width390/bottom844 on viewport height844; conditions finish precisely at lineup start. 10-draw button checked the 10 radio. Temporary viewport restored. Existing sample wallet/animation/result behavior retained.
